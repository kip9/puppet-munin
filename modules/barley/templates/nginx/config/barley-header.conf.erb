### FILE MANAGED BY PUPPET ###
    root          /var/www/barley.plain;
    index         index.php index.html index.htm;
    access_log    logs/barley.plain/access.log main;
    error_log     logs/barley.plain/error.log;

    # Add custom headers
    add_header    X-ENV staging;
    add_header    X-Server $hostname;
    add_header    X-Hack "Hey you, obviously you are sneaky. Contact me at jeff@getbarley.com if you are interested in chatting.";
    add_header    X-Frame-Options DENY;
